// NOTE:    for now we just gunna generate cpp source code and the use the
//              cpp compiler to compile the cpp source into machine code exe.

#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
